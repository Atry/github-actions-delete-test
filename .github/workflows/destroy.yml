on:
  delete:
    branches:
      - "**"

jobs:
  destroy:
    runs-on: ubuntu-latest
    env:
      WORKSPACE_NAME: ${{github.ref_name}}
    steps:
      - uses: actions/checkout@v2.4.0
      - run: find .
